#include <iostream>
#include <string>
#include <cstdlib>
#include <vector>
#include <sstream>
using namespace std;

int main() {

    //template for user input in a line separated by spaces. Adds all input into a vector.
    string input;
    int height;
    int base;

    //WHILE LOOP METHOD WITH UNERTAIN NUMBER OF INPUTS.
    while (getline(cin, input, '\n')) { // use delim '\n' if inputs are separated by line. use delim ' ' if inputs are separated by spaces

        
        cout << input << endl;
        
        

    } 

    return 0;

}